const PRODUCTS=[
{id:'laser-panel',name:'CNC Laser Cut Panels',category:'exterior',label:'Exteriors',image:'laser-panel.webp',badge:'CUSTOM DESIGN',description:'Decorative laser-cut panels for entrances, partitions and architectural accents. Designed around your space, pattern preference and finish.',guidance:'Your approximate width and height, preferred pattern or reference image, material or finish, and where the panel will be installed.'},
{id:'front-elevation',name:'Front Elevation',category:'exterior',label:'Exteriors',image:'front-elevation.webp',badge:'MADE TO MEASURE',description:'Give your building a distinct first impression with front elevation fabrication tailored to the architecture and your vision.',guidance:'A photo or drawing of the building, approximate façade dimensions, the look you want, and site location.'},
{id:'aluminium-window',name:'Aluminium Windows',category:'windows',label:'Windows',image:'aluminium-window.webp',badge:'CUSTOM SIZE',description:'Clean-lined aluminium window solutions planned for your opening, preferred configuration and frame finish.',guidance:'Opening width and height, number of windows, preferred opening style and frame colour or finish.'},
{id:'upvc-window',name:'uPVC Windows',category:'windows',label:'Windows',image:'upvc-window.webp',badge:'CUSTOM SIZE',description:'uPVC windows shaped around the opening and the way you use your room. Share the size and configuration you need.',guidance:'Approximate opening width and height, number of windows, preferred opening style and installation location.'},
{id:'rolling-shutter',name:'Rolling Shutters',category:'exterior',label:'Exteriors',image:'rolling-shutter.webp',badge:'CUSTOM SIZE',description:'Rolling shutter fabrication for shopfronts, garages and other openings, made to suit the site dimensions.',guidance:'Opening width and height, installation location and any preferred finish or operating requirements.'},
{id:'ss-railing',name:'Stainless Steel Railings',category:'exterior',label:'Exteriors',image:'ss-railing.webp',badge:'CUSTOM DESIGN',description:'Stainless steel railing work for stairs, balconies and terraces, with the proportions and style suited to your space.',guidance:'Where the railing is needed, approximate running length and height, and a style reference if you have one.'},
{id:'modular-kitchen',name:'Modular Kitchens',category:'interiors',label:'Interiors',image:'modular-kitchen.webp',badge:'CUSTOM PROJECT',description:'Kitchen planning and fabrication made around your room, storage needs and the finish you have in mind.',guidance:'Room dimensions, layout sketch or photos, storage requirements and preferred colour or material finish.'},
{id:'tin-shade',name:'Tin Shades & Fabrication',category:'exterior',label:'Exteriors',image:'tin-shade.webp',badge:'CUSTOM BUILD',description:'Tin shade and other fabrication work planned for the coverage, structure and finish your site needs.',guidance:'The area to cover, approximate dimensions, site photos and your preferred material or structure.'}
];
const $=s=>document.querySelector(s),escapeHTML=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const productURL=p=>`product.html?id=${encodeURIComponent(p.id)}`;
const imageURL=p=>`assets/${p.image}`;
const storageKey='himalayan-designers-cart-v1';
let cart=[],lastFocus=null;
try{const saved=JSON.parse(localStorage.getItem(storageKey)||'[]');if(Array.isArray(saved))cart=saved.filter(item=>PRODUCTS.some(p=>p.id===item.id)&&Number.isInteger(item.qty)&&item.qty>0&&item.qty<=99).map(item=>({id:item.id,qty:item.qty,size:String(item.size||'').slice(0,120),finish:String(item.finish||'').slice(0,120)}))}catch(e){cart=[]}
function persist(){try{localStorage.setItem(storageKey,JSON.stringify(cart))}catch(e){}updateCount()}
function updateCount(){const count=cart.reduce((n,item)=>n+item.qty,0);$('#cartCount').textContent=count;$('#drawerCount').textContent=`(${count})`}
function toast(text){const el=$('#toast');el.textContent=text;el.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>el.classList.remove('show'),2500)}
function card(p,featured=false){return `<article class="product-card${featured?' featured':''}"><a class="product-photo" href="${productURL(p)}" aria-label="View ${escapeHTML(p.name)} details"><img src="${featured?'assets/hero-facade.webp':imageURL(p)}" alt="Design inspiration for ${escapeHTML(p.name)}" loading="lazy"><span class="image-tag">${escapeHTML(p.badge)}</span><span class="round-arrow" aria-hidden="true">↗</span></a><div class="product-body"><div><p class="meta">${escapeHTML(p.label)}</p><a href="${productURL(p)}"><h3>${escapeHTML(p.name)}</h3></a></div><p class="price-label">Custom quote</p></div></article>`}
function renderHome(){let filter='all',query='';const grid=$('#productGrid');function draw(){const list=PRODUCTS.filter(p=>(filter==='all'||p.category===filter)&&(`${p.name} ${p.label} ${p.description}`.toLowerCase().includes(query)));grid.classList.toggle('full-collection',filter==='all'&&!query);grid.innerHTML=list.map((p,i)=>card(p,filter==='all'&&!query&&i===0)).join('');$('#noResults').hidden=!!list.length}$('#filters').addEventListener('click',e=>{const button=e.target.closest('[data-filter]');if(!button)return;filter=button.dataset.filter;document.querySelectorAll('.filters button').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button))});draw()});$('#search').addEventListener('input',e=>{query=e.target.value.trim().toLowerCase();draw()});draw()}
function renderDetail(){const id=new URLSearchParams(window.location.search).get('id');const p=PRODUCTS.find(item=>item.id===id);if(!p){window.location.replace('collection.html');return}document.title=`${p.name} | Himalayan Designer’s`;$('#crumbTitle').textContent=p.name;$('#detailTitle').textContent=p.name;$('#detailDescription').textContent=p.description;$('#detailGuidance').textContent=p.guidance;$('#detailCategory').innerHTML='<i></i> '+escapeHTML(p.label.toUpperCase());$('#detailImage').src=imageURL(p);$('#detailImage').alt=`Design inspiration for ${p.name}`;$('#galleryIndex').textContent=String(PRODUCTS.indexOf(p)+1).padStart(2,'0')+' / 08';$('#relatedGrid').innerHTML=PRODUCTS.filter(item=>item.id!==p.id).slice(0,3).map(item=>card(item)).join('');$('#productForm').addEventListener('submit',e=>{e.preventDefault();const qty=Math.min(99,Math.max(1,parseInt($('#qtyInput').value,10)||1));const size=$('#sizeInput').value.trim().slice(0,120),finish=$('#finishInput').value.trim().slice(0,120);const existing=cart.find(item=>item.id===p.id&&item.size===size&&item.finish===finish);if(existing)existing.qty=Math.min(99,existing.qty+qty);else cart.push({id:p.id,qty,size,finish});persist();openCart();toast('Added to your cart')})}
function openCart(){lastFocus=document.activeElement;renderCart();$('#cartDrawer').classList.add('open');$('#cartDrawer').setAttribute('aria-hidden','false');$('#overlay').hidden=false;document.body.classList.add('locked');$('#closeCart').focus()}
function closeCart(){$('#cartDrawer').classList.remove('open');$('#cartDrawer').setAttribute('aria-hidden','true');$('#overlay').hidden=true;document.body.classList.remove('locked');lastFocus?.focus?.()}
function renderCart(){const el=$('#cartItems');$('#checkout').hidden=!cart.length;if(!cart.length){el.innerHTML='<div class="empty-cart"><div class="empty-icon">◇</div><h3>Your cart is empty.</h3><p>Explore the collection and choose what fits your project.</p><a class="btn btn-dark" href="collection.html">Explore collection <span>↗</span></a></div>';return}el.innerHTML=cart.map((item,i)=>{const p=PRODUCTS.find(x=>x.id===item.id);return `<article class="cart-item"><a href="${productURL(p)}"><img src="${imageURL(p)}" alt=""></a><div><a href="${productURL(p)}"><h3>${escapeHTML(p.name)}</h3></a>${item.size?`<p>Size: ${escapeHTML(item.size)}</p>`:''}${item.finish?`<p>Finish: ${escapeHTML(item.finish)}</p>`:''}<p>Custom quote</p><div class="cart-actions"><div class="mini-qty"><button type="button" data-index="${i}" data-step="-1" aria-label="Decrease ${escapeHTML(p.name)} quantity">−</button><span>${item.qty}</span><button type="button" data-index="${i}" data-step="1" aria-label="Increase ${escapeHTML(p.name)} quantity">+</button></div><button class="remove-item" type="button" data-remove="${i}">Remove</button></div></div></article>`}).join('')}
function checkout(e){e.preventDefault();if(!cart.length)return;const form=e.currentTarget;if(!form.reportValidity())return;const fd=new FormData(form),name=String(fd.get('name')||'').trim(),phone=String(fd.get('phone')||'').trim(),address=String(fd.get('address')||'').trim(),notes=String(fd.get('notes')||'').trim();if(!name||!phone||!address)return;const lines=['Hello Himalayan Designer’s, I would like to place an order request:','',...cart.flatMap((item,i)=>{const p=PRODUCTS.find(x=>x.id===item.id);return [`${i+1}. ${p.name} — Qty: ${item.qty}`,...(item.size?[`   Approx. size: ${item.size}`]:[]),...(item.finish?[`   Material / finish: ${item.finish}`]:[])]}),'',`Name: ${name}`,`Phone: ${phone}`,`Site / delivery location: ${address}`,...(notes?[`Project details: ${notes}`]:[]),'','Please confirm measurements, final price and next steps.'];window.location.href='https://wa.me/919634888987?text='+encodeURIComponent(lines.join('\n'))}
document.addEventListener('click',e=>{const qty=e.target.closest('[data-index]');if(qty){const i=Number(qty.dataset.index);if(cart[i]){cart[i].qty+=Number(qty.dataset.step);if(cart[i].qty<=0)cart.splice(i,1);persist();renderCart()}}const remove=e.target.closest('[data-remove]');if(remove){cart.splice(Number(remove.dataset.remove),1);persist();renderCart()}});
const menuButton=$('#menuOpen'),mobileNav=$('#mobileNav');function closeMenu(){mobileNav.hidden=true;menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Open menu')}menuButton.addEventListener('click',()=>{const opening=mobileNav.hidden;mobileNav.hidden=!opening;menuButton.setAttribute('aria-expanded',String(opening));menuButton.setAttribute('aria-label',opening?'Close menu':'Open menu')});mobileNav.addEventListener('click',e=>{if(e.target.closest('a'))closeMenu()});$('#openCart').addEventListener('click',()=>{closeMenu();openCart()});$('#closeCart').addEventListener('click',closeCart);$('#overlay').addEventListener('click',closeCart);$('#checkoutForm').addEventListener('submit',checkout);document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeMenu();if($('#cartDrawer').classList.contains('open'))closeCart()}});$('#year').textContent=new Date().getFullYear();updateCount();const currentPage=document.body.dataset.page;document.querySelectorAll('.nav-links a, .mobile-nav a').forEach(a=>{if(a.getAttribute('href')===`${currentPage==='product'?'collection':currentPage}.html`)a.setAttribute('aria-current','page')});if(currentPage==='home'||currentPage==='collection')renderHome();else if(currentPage==='product')renderDetail();

// Decorative cursor cue for desktop pointers. It never replaces or blocks the native cursor.
if(typeof window.matchMedia==='function'&&window.matchMedia('(pointer:fine) and (prefers-reduced-motion:no-preference)').matches){
  const ring=document.createElement('div');ring.className='cursor-ring';ring.setAttribute('aria-hidden','true');document.body.appendChild(ring);
  document.addEventListener('pointermove',event=>{ring.style.left=`${event.clientX}px`;ring.style.top=`${event.clientY}px`;ring.classList.add('is-visible');ring.classList.toggle('is-action',!!event.target.closest?.('a,button,[role="button"]'))},{passive:true});
  document.addEventListener('pointerout',event=>{if(!event.relatedTarget)ring.classList.remove('is-visible')});
  window.addEventListener('blur',()=>ring.classList.remove('is-visible'));
}
